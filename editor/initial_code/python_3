def cota_megabytes(X, N, uso_mensal):
    # escreva aqui seu código
    return None


print("Example:")
print(cota_megabytes(200, 2, [150, 220]))

# As funções assert são utilizadas para verificar se o resultado da função chamada é o esperado
# Teste 1
assert cota_megabytes(200, 2, [150, 220]) == 230

# Teste 2
assert cota_megabytes(100, 3, [50, 80, 30]) == 240

# Teste 3
assert cota_megabytes(150, 1, [120]) == 180

print("Seu código passou pelos testes, clique em Check para avançar!")
